# nestjs-logging-elk

### How to run
```bash
docker-compose build --parallel
docker-compose up -d
```

### Ports
 * ELASTIC_PORT (default - 9200) - Elasticsearch
 * KIBANA_PORT (default - 5601) - Kibana